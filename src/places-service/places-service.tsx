import { Place } from "../dtos/place";

// returns autocomplete of places
export function getPlaces(placeName: string): Place[] {
    return []
}

// gets a Google Map place by its reference Id
export function getPlace(placeReferenceId: string): Place {
    return {}
}

